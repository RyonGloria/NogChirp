% 构建一个用于记录解调器解调过程中间变量的结构体
function decoderRecord = decoderStruct()
    decoderRecord = [];
    decoderRecord.loraSet = []; % 配置参数
    decoderRecord.OverBandBw = 0; % 过渡带带宽
    decoderRecord.channelList = [];
    decoderRecord.preambleUpchirp = [];
    decoderRecord.preambleDownchirp = [];
    decoderRecord.activeChannel = 0;
    decoderRecord.maxAmp = 0;
    decoderRecord.maxBin = 0;
    decoderRecord.PreambleStartPos
    decoderRecord.PreambleNum
    decoderRecord.PreambleBin
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
    decoderRecord.
end